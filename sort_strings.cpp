//#include <iostream>
//#include <string>
//#include <vector>
//#include <algorithm>
//
//int main(void) {
//	std::string a, b, c;
//	std::vector<std::string> strings;
//	std::cin >> a >> b >> c;
//	if (a.size()) strings.push_back(a);
//	if (b.size()) strings.push_back(b);
//	if (c.size()) strings.push_back(c);
//	std::sort(strings.begin(), strings.end());
//	std::cout << strings[0];
//	return (0);
//}